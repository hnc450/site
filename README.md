# Documentation du site sur Github 

## Initialisation du depot avec git

```
    git ini
    git remote add origin  ssh_origin
    git log --online pour 
    git commit --amend 
    git branch -b branche_name

```

## Manipulation de l historique  des commits 

```
  git commit --amend
```

## Changer de branche 

```
   git checkout branche_name
```