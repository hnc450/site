name: Deployement-to-zetsu-account
on:
  push: 
    branches:
      - main
permissions: 
  contents: write
jobs:
  deploy:
    name: run-on-machine
    runs-on: ubuntu-24.04

    steps:
      - name: chekout-repository-code-in-vm
        uses: actions/checkout@v4

      - name: ssh-deployement
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.IP }}
          username: ${{ secrets.USER }}
          password: ${{ secrets.PASSWORD }}
          port: 22
          script: |
            cd /home/${{ secrets.USER }}/Desktop/
            rm -rf blog-zetsu.duckdns.org
            git clone https://github.com/hnc450/site.git blog-zetsu.duckdns.org
            cd blog-zetsu.duckdns.org
            rm -rf .git
            rm -rf .github
            rm -rf *.md
            cd ..
            echo ${{ secrets.PASSWORD }} | sudo -S rm -rf /var/www/blog-zetsu.duckdns.org/
            echo ${{ secrets.PASSWORD }} | sudo -S mv blog-zetsu.duckdns.org /var/www/
           
            
          
